import { Injectable } from '@nestjs/common';

//@Injectable()
export interface IUsersServices {
  // createUser(body: any): Promise<any>;
  // findUserByEmail(email: string, isActive: boolean): Promise<any>;
  // findUserById(id: string, isActive: boolean): Promise<any>;
  // updateUser(id: string, body: any): Promise<any>;
  // deleteUser(id: string): Promise<any>;
  // makeAdmin(id: string, body: any): Promise<any>;
  // resetPassword(body: any): Promise<any>;
  // sendEmailActivateAccount(id: string, email: string): Promise<any>;
}
